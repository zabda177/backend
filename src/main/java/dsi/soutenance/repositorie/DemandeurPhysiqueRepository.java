package dsi.soutenance.repositorie;

import dsi.soutenance.model.DemandeurPhysique;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DemandeurPhysiqueRepository extends JpaRepository<DemandeurPhysique, Long> {
}
